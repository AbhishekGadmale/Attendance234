1. Put this project in a GitHub repo.
2. Create a Render.com Web Service (Connect GitHub).
3. Build command: pip install -r requirements.txt
4. Start command: gunicorn app:app
5. Deploy and open URL. Use webcam permission in browser.

Note: Model trains locally on server from uploaded images. If model file not present, add images via Register UI then wait for training.
